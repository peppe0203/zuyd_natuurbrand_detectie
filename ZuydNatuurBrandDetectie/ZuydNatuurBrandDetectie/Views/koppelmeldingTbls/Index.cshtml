@model IEnumerable<ZuydNatuurBrandDetectie.Models.koppelmeldingTbl>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.meldingenTbl.type)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nodeTbl.locatie)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.meldingenTbl.type)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nodeTbl.locatie)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
